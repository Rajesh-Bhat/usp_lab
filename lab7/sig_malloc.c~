#include <stdio.h>
#include <unistd.h>
#include <signal.h>
struct hello
{
	int a;
	int b;
	int c;
}data;


void sighandler(int sig)
{
	
}
int main()
{
	char *str;
	signal(SIGALRM,sighandler);	
	alarm(2);
	
	while(1)
	{
		str = (char *)malloc(15);
	}
	
	
}
